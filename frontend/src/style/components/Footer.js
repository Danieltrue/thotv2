import styled from "styled-components";

const FooterStyle = styled.footer`
  padding: 0.5rem 1rem;
  font-size: 50%;
  position: absolute;
  bottom: 0;
  right: 0;
  left: 0;
  .footer__inner {
    ul {
      li {
        margin-left: 1rem;
      }
    }
  }
`;

export default FooterStyle;
